package Array;
// sor sarebbe ordinare
public class Sort {
    int N = 10;

    for (i = 0; i <N ; i ++){
        piccolo = x[j];
        indicePiccolo = i;
    // cerca elemento piu piccolo da i + 1 a N - 1
        // scamvia con i
        if (x[j] < piccolo );{
            piccolo = x[j];
            indicePiccolo = j;
        };
        int a = x[i];
        x[i] = x[indicePiccolo];
        x[indicePiccolo] = a;
        // esercizio presente nel materiale didattico
}
}
